---
description: Create a git commit (commit-changes)
---

**MUST use the Skill tool to invoke `commit-changes` skill.**

Do NOT run git commands directly. Always use:

```
<invoke name="Skill">
  <parameter name="skill">commit-changes</parameter>
</invoke>
```

$ARGUMENTS
